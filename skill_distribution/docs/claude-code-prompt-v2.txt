你是一个技术调研助手。请帮我调研 GitHub 上 PDF/OCR/发票识别相关的热门开源工具，对比我们现有的技能找提升空间。

═══════════════════════════════════════════
## 📌 背景信息（必须基于以下事实给出结论）

### 我们的现有技能体系
1. **markitdown**（已安装）— 万能格式转换：PDF/Word/PPT/Excel/图片→Markdown
2. **pymupdf / pymupdf4llm**（已安装）— PDF 基础处理：文本提取、页面操作
3. **vision_analyze 工具**（已安装）— 图片识别由辅助模型完成
4. **marker-pdf**（已安装）— 扫描件转 Markdown，内置 OCR
5. **invoice-pdf-automation**（自研技能）— 发票 PDF 批量处理流水线

### 当前发票流程
- 发票 PDF → markitdown 转 Markdown → vision_analyze 图片识别 → 人工整理 → Excel
- 痛点：vision_analyze 成本高、速度慢；扫描件识别质量不稳定；没有结构化 schema

### 约束条件
- 平台：macOS
- Python：请检查实际版本
- Hermes 工作目录：~/HermesPet_Workspace
- 要求：能集成到 Hermes 工具链，或者至少能当 CLI 工具用
- 成本敏感：尽量少用付费 API

### 目标
- 找 3-5 个工具/方案，补我们的能力缺口
- 每个方案说清楚：解决什么痛点、怎么装、对我们现有技能的补充/替代关系

═══════════════════════════════════════════

## 步骤 1：用 GitHub API 搜索三个领域（按星标排序）

运行以下命令，输出所有结果，不要省略：

```bash
# PDF 提取
echo "=== PDF 提取 ===" && curl -s "https://api.github.com/search/repositories?q=pdf+extraction+OR+pdf-parse&sort=stars&per_page=10" | python3 -c "
import sys, json
data = json.loads(sys.stdin.read())
for r in data.get('items', [])[:10]:
    print(f\"{r['full_name']} | {r['stargazers_count']}⭐ | {r['language']} | {r['description'][:100] if r['description'] else 'N/A'}\")
"
```

```bash
# OCR / 图片识别
echo "=== OCR/图片识别 ===" && curl -s "https://api.github.com/search/repositories?q=OCR+document+intelligence+OR+image+recognition+OR+document+OCR&sort=stars&per_page=10" | python3 -c "
import sys, json
data = json.loads(sys.stdin.read())
for r in data.get('items', [])[:10]:
    print(f\"{r['full_name']} | {r['stargazers_count']}⭐ | {r['language']} | {r['description'][:100] if r['description'] else 'N/A'}\")
"
```

```bash
# 发票识别
echo "=== 发票识别 ===" && curl -s "https://api.github.com/search/repositories?q=invoice+extraction+OR+invoice-OCR+OR+invoice+processor&sort=stars&per_page=10" | python3 -c "
import sys, json
data = json.loads(sys.stdin.read())
for r in data.get('items', [])[:10]:
    print(f\"{r['full_name']} | {r['stargazers_count']}⭐ | {r['language']} | {r['description'][:100] if r['description'] else 'N/A'}\")
"
```

## 步骤 2：对每个领域排名前 3 的 repo，抓取 README 前 50 行

比如：
```bash
curl -sL "https://raw.githubusercontent.com/opendataloader-project/opendataloader-pdf/main/README.md" | head -50
```

## 步骤 3：检查当前环境

```bash
echo "=== Python ===" && python3 --version
echo "=== markitdown ===" && python3 -c "import markitdown; print('version:', markitdown.__version__)" 2>/dev/null || echo "未安装"
echo "=== pymupdf ===" && python3 -c "import pymupdf; print('OK')" 2>/dev/null || echo "未安装"
echo "=== marker ===" && pip3 list 2>/dev/null | grep -iE "marker|pymupdf|markitdown|pdfminer"
```

## 步骤 4：整理结构化报告

每个工具必须说：
- 工具名、星标数、语言、核心能力
- **Python 支持？CLI？MCP Server？**
- **安装方式**
- **对我们现有技能的补充/替代关系**

## 验收清单（你必须全部完成，缺一不可）

- [ ] 步骤1：三个领域各搜 10 个结果，完整输出
- [ ] 步骤2：每个领域 Top 3 的 README 前 50 行
- [ ] 步骤3：环境检查完整输出
- [ ] 步骤4：结论包含"对现有技能的补充/替代关系"

**如果任何一步没做到，不要提交最终报告，先补做。**
