你是一个技术调研助手。请帮我调研 GitHub 上 PDF/OCR/发票识别相关的热门开源工具，对比我们现有的技能找提升空间。

═══════════════════════════════════════════
## 📌 背景信息（必须基于以下事实给出结论）

### 我们的现有技能体系
1. **markitdown**（已安装）— 万能格式转换：PDF/Word/PPT/Excel/图片→Markdown，149k⭐
2. **pymupdf / pymupdf4llm**（已安装）— PDF 基础处理：文本提取、页面操作
3. **vision_analyze 工具**（已安装）— 图片识别由辅助模型 agnes-2.0-flash-vision 完成
4. **marker-pdf**（已安装）— 扫描件转 Markdown，内置 OCR
5. **invoice-pdf-automation**（自研技能）— 发票 PDF 批量处理流水线

### 当前发票流程
- 发票 PDF → markitdown 转 Markdown → vision_analyze 图片识别 → 人工整理 → Excel
- PDF 命名规则：`YYYYMMDDHHMMSS-分类-收款方-付款方-金额.pdf`
- 痛点：
  - vision_analyze 调用成本高、速度慢
  - 扫描件 PDF 识别质量不稳定
  - 没有结构化 schema，每次输出格式可能不同
  - 没有 PDF 智能路由（不知道扫描件还是文本型）

### 约束条件
- 平台：macOS
- Python：3.11
- Hermes 工作目录：~/HermesPet_Workspace
- 要求：能集成到 Hermes 工具链，或者至少能当 CLI 工具用
- 成本敏感：尽量少用付费 API

### 目标
- 找 3-5 个工具/方案，补我们的能力缺口
- 每个方案说清楚：解决什么痛点、怎么装、对我们现有技能的补充/替代关系
- 给出明确的优先级（立即/中期/不建议）

═══════════════════════════════════════════

## 步骤 1：用 GitHub API 搜索三个领域（按星标排序）

运行以下命令：

```bash
# PDF 提取
curl -s "https://api.github.com/search/repositories?q=pdf+extraction+OR+pdf-parse&sort=stars&per_page=10" | python3 -c "
import sys, json
data = json.loads(sys.stdin.read())
for r in data.get('items', [])[:10]:
    print(f\"{r['full_name']} | {r['stargazers_count']}⭐ | {r['language']} | {r['description'][:100] if r['description'] else 'N/A'}\")
"
```

```bash
# OCR / 图片识别
curl -s "https://api.github.com/search/repositories?q=OCR+document+intelligence+OR+image+recognition+OR+document+OCR&sort=stars&per_page=10" | python3 -c "
import sys, json
data = json.loads(sys.stdin.read())
for r in data.get('items', [])[:10]:
    print(f\"{r['full_name']} | {r['stargazers_count']}⭐ | {r['language']} | {r['description'][:100] if r['description'] else 'N/A'}\")
"
```

```bash
# 发票识别
curl -s "https://api.github.com/search/repositories?q=invoice+extraction+OR+invoice-OCR+OR+invoice+processor&sort=stars&per_page=10" | python3 -c "
import sys, json
data = json.loads(sys.stdin.read())
for r in data.get('items', [])[:10]:
    print(f\"{r['full_name']} | {r['stargazers_count']}⭐ | {r['language']} | {r['description'][:100] if r['description'] else 'N/A'}\")
"
```

## 步骤 2：对排名前三的 repo，抓取 README 前 50 行

## 步骤 3：检查当前环境

```bash
python3 --version
python3 -c "import markitdown; print('markitdown:', markitdown.__version__)" 2>/dev/null || echo "未安装"
python3 -c "import pymupdf; print('pymupdf: OK')" 2>/dev/null || echo "未安装"
pip3 list 2>/dev/null | grep -iE "marker|pymupdf|markitdown|pdfminer"
```

## 输出要求

整理成一份结构化报告，每个工具必须说：
- 工具名、星标数、语言、核心能力
- **Python 支持？CLI？MCP Server？**
- **安装方式**
- **对我们现有技能的补充/替代关系**（这个最重要！）

示例输出格式：

```
## PDF 提取工具
1. [工具] | ⭐[x] | [语言] | [核心能力]
   - Python 支持：有/无
   - CLI/MCP：有/无
   - 对我们的价值：补充 markitdown 的表格提取能力 / 替代 vision_analyze 的部分场景
   - 安装：pip install xxx / brew install xxx
   - 推荐优先级：🔥高 / 🟡中 / ❌不推荐

## OCR/图片识别工具
...

## 发票识别工具
...

## 当前环境
- markitdown: x.x.x
- pymupdf: OK
- marker-pdf: OK

## 推荐的 3 个候选工具
1. [工具] - [优先级]
   - 解决什么痛点
   - 安装方式
   - 代价/风险

## 总结
一句话结论：我们最需要补充什么能力，最快落地的方案是什么。
```

只输出事实和数据，不要编造。每个结论必须基于实际搜索结果，且必须与我们的现有技能体系对齐。
